# AICut 依赖
# 安装：pip install -r requirements.txt
# 需额外安装 ffmpeg（见 README）

# --- Web 框架 ---
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.0

# --- 语音识别 ---
openai-whisper>=20231117  # 自动拉 torch + numpy

# --- 工具 ---
numpy>=1.24.0
